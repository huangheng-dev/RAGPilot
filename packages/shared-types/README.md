# Shared Types

This package is the reserved repository boundary for generated or versioned cross-service contracts, such as:

- generated OpenAPI schemas
- TypeScript DTOs used by the web app
- event payload contracts for worker integration

No generated contract artifact is currently published from this package. Existing Web/API contracts remain application-local until a generation or shared-versioning workflow is introduced and enforced in CI.
